export class PadInfo {
  constructor(note: string, row: number, label: string, column: number) {
    this.note = note;
    this.row = row;
    this.label = label;
    this.column = column;
  }
  note: string;
  row: number;
  label: string;
  column: number;
}


