/*
import {Instrument} from "../../mip/instruments/Instrument";
import {Sample} from "../Sample";

export class SampledInstrument implements Instrument{

  private samples:Array<{noteTrigger:string,articulation:string,sample:Sample}>=[];

  addSample(noteTrigger:string,articulation:string,sample:Sample):void{
    this.samples.push({noteTrigger:noteTrigger,articulation:articulation,sample:sample});
  }

  getName(): string {
    return "";
  }

  play(note: string, time: number, length: number, loudness: number, articulation?: number): void {
  }

}
*/
