export class TriggerSpec{
  constructor(note: string, label: string, iconUrl: string) {
    this.note = note;
    this.label = label;
    this.iconUrl = iconUrl;
  }

  note: string;
  label:string;
  iconUrl:string;

}
